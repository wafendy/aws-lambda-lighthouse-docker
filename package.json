{
  "name": "aws-lambda-lighthouse-docker",
  "version": "1.0.0",
  "description": "AWS Image Container for running Google Lighthouse in Lambda",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-aws-lambda": "^9.1.0",
    "lighthouse": "^7.4.0",
    "lighthouse-logger": "^1.2.0",
    "puppeteer-core": "^9.1.1"
  }
}
